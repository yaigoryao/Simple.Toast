<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Simple.TestApp.MainPage"
             xmlns:simple="clr-namespace:Simple.Toast;assembly=Simple.Toast"
             xmlns:local="clr-namespace:Simple.TestApp"
             x:DataType="local:MainPageVM">
    <Grid RowDefinitions="*"
          ColumnDefinitions="*">

                    <!--DismissDelay: toast dismiss delay, MS
                    Show: boolean property, showing toast when true
                    Dismiss: boolean property, closing toast when true
                    ToastDirection: enumerable property, switches toast appearance direction
                    Important: set ZIndex property value-->
        <simple:Toast DismissDelay="2000" 
                      ToastBackgroundColor="#2E7D32" 
                      CornerRadius="12"
                      ToastPadding="16,12"
                      ProgressBarColor="#81C784"
                      Show="{Binding Show}" 
                      Dismiss="{Binding Dismiss}"
                      ToastDirection="LeftToRight"
                      VerticalOptions="Start"
                      HorizontalOptions="Center"
                      ZIndex="5">
            <simple:Toast.InnerContent> <!--Toast inner content-->
                <Label Text="✓ Operation completed successfully!"
                       TextColor="White"
                       FontSize="16"
                       FontAttributes="Bold"
                       HorizontalTextAlignment="Center"
                       VerticalTextAlignment="Center" />
            </simple:Toast.InnerContent>
        </simple:Toast>

        <ScrollView Grid.Row="0"
                    Grid.Column="0">
            <VerticalStackLayout Padding="30,0"
                                 Spacing="25">
                <Image Source="dotnet_bot.png"
                       HeightRequest="185"
                       Aspect="AspectFit"
                       SemanticProperties.Description="dot net bot in a hovercraft number nine" />

                <Label Text="Hello, World!"
                       Style="{StaticResource Headline}"
                       SemanticProperties.HeadingLevel="Level1" />

                <Label Text="Welcome to &#10;.NET Multi-platform App UI"
                       Style="{StaticResource SubHeadline}"
                       SemanticProperties.HeadingLevel="Level2"
                       SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

                <Button x:Name="CounterBtn"
                        Text="Click me"
                        SemanticProperties.Hint="Counts the number of times you click"
                        Clicked="OnCounterClicked"
                        HorizontalOptions="Fill" />

                <Button Text="Send notification"
                        HorizontalOptions="Fill"
                        Command="{Binding ShowNotificationCommand}" />
                
                <Button Text="Cancel notification"
                        HorizontalOptions="Fill"
                        Command="{Binding CancelNotificationCommand}" />
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
